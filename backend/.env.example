
MONGO_URI=your_mongodb_connection_string
JWT_SECRET=your_jwt_secret

PORT=5000

# Deployed frontend URL(s), comma-separated, used to allow CORS requests (no trailing slash, no wildcard)
CLIENT_URL=https://ecommerce-frontend-git-main-aayush-s-projects13.vercel.app

# Cloudinary (used to host product images so they work on Vercel's serverless backend)
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret
